export const getInitialized = (state) => {
  return state.app.initialized;
};

export const getPostsLinkIsEnabled = (state) => {
  return state.app.postsLinkIsEnabled;
};

export const getPostLinkIsEnabled = (state) => {
  return state.app.postLinkIsEnabled;
};
